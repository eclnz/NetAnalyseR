% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_nodal_metrics.R
\name{compute_nodal_metrics}
\alias{compute_nodal_metrics}
\title{Compute Nodal Metrics for Brain Network Data}
\usage{
compute_nodal_metrics(matrices_array, nodal_metrics, subject_names = NULL)
}
\arguments{
\item{matrices_array}{An array where each slice represents a connectivity matrix for a subject.}

\item{nodal_metrics}{A vector of strings specifying the nodal metrics to be calculated.}

\item{subject_names}{An optional vector of subject identifiers. If not provided, subjects will be named
sequentially as Subject1, Subject2, etc.}
}
\value{
A long-format data frame containing the calculated nodal metrics for each node and subject.
Each row corresponds to a node-subject combination with columns for node number, subject identifier, and the
calculated metric values.
}
\description{
This function calculates specified nodal metrics for a given set of brain network matrices
for multiple subjects. It computes the specified metrics for each node in each subject's brain network.
}
\examples{
W <- matrix(c(0, 2, 1, 4, 2, 0, 3, 5, 1, 3, 0, 6, 4, 5, 6, 0), nrow = 4, byrow = TRUE)
matrices_array <- array(W, dim = c(3, 3, 1))
nodal_metrics <- c("local_clustering_coefficient_wei", "local_efficiency_wei")
subject_names <- c("Subject1")
metrics_df <- compute_nodal_metrics(matrices_array, nodal_metrics, subject_names)

}
